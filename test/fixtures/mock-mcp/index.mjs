#!/usr/bin/env node
/**
 * Minimal mock MCP server (skeleton).
 * TODO: implement tools/list + echo tool over stdio.
 */
console.error("[mock-mcp] skeleton — not yet serving MCP");
process.stdin.resume();
