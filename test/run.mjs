#!/usr/bin/env node
/**
 * Integration test entry (skeleton).
 * TODO: spawn Probe/Gate against mock MCP backends via stdio.
 */
console.log("[costgate-test] skeleton — no tests yet");
process.exit(0);
