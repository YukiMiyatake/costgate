#!/usr/bin/env node
/**
 * CostGate Probe — MCP measurement proxy (scaffold)
 *
 * TODO: stdio proxy, metrics logging, backend delegation
 */
console.error(
  "[costgate-probe] Scaffold v0.1.0 — proxy implementation coming soon."
);
console.error(
  "[costgate-probe] See docs/log-schema.md for the metrics format."
);
process.exit(0);
