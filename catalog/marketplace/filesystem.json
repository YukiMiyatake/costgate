{
  "id": "filesystem",
  "name": "Filesystem MCP",
  "description": "Read and write local files within allowed directories via @modelcontextprotocol/server-filesystem. Gate-backed via backends.json.",
  "category": "filesystem",
  "category_label": "Filesystem",
  "official": true,
  "gate_ready": true,
  "popularity": "high",
  "docs_url": "https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem",
  "tags": ["filesystem", "files", "local", "read", "write"],
  "tier_catalog": "filesystem",
  "install_target": "backend",
  "backend_key": "filesystem",
  "backend_template": {
    "always": true,
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "${ALLOWED_PATH}"],
    "env": {}
  },
  "required_env": [
    {
      "name": "ALLOWED_PATH",
      "description": "MCP が読み書きできるディレクトリ（絶対パス）。通常はプロジェクトルート",
      "secret": false,
      "path_suggestions": true
    }
  ],
  "compare_estimate": {
    "tool_count": 9,
    "before_tokens": 720,
    "after_tokens": 380,
    "source": "filesystem mock smoke — tier filter"
  }
}
